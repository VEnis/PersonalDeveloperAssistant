language: python
python:
  - "3.3"
install:
  - pip install -r requirements.txt --use-mirrors #General requirements
#  - pip install pinocchio # nose plugins for testing
#  - pip install coverage  # nose plugins for testing
script: nose2
